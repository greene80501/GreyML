"""Package metadata entry point.
Defines how the Python package is built and installed for GreyML.
"""

from setuptools import setup

setup(name="greyml-placeholder", version="0.0.0", packages=["greyml"])
